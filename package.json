{
  "name": "random-fact-api",
  "version": "1.0.0",
  "description": "A simple RESTful API that returns interesting random facts using Node.js and Express. It includes endpoints to fetch all facts, retrieve a random fact, and get a fact by ID, demonstrating basic API design and deployment.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev":"nodemon server.js"
  },
  "repository": {
    "type": "git",
    "url": "y"
  },
  "author": "Rahul Raj",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
